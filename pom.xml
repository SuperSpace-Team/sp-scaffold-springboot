<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.sp.scaffold</groupId>
    <artifactId>{{service.code}}</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0-SNAPSHOT</version>
    <description>{{service.code}} project</description>

    <modules>
        <module>{{service.code.lower}}-feign</module>
        <module>{{service.code.lower}}-api</module>
        <module>{{service.code.lower}}-dao</module>
        <module>{{service.code.lower}}-service</module>
        <module>{{service.code.lower}}-server</module>
    </modules>

    <!-- 依赖基础组件工具集(包含常用二方库、开源组件包父POM声明集sp-base-dependencies) -->
    <parent>
        <groupId>com.sp.infra.comp</groupId>
        <artifactId>sp-infra-comp-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <properties>
        <project.version>1.0.0-SNAPSHOT</project.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.sp.scaffold</groupId>
                <artifactId>{{service.code.lower}}-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.sp.scaffold</groupId>
                <artifactId>{{service.code.lower}}-feign</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.sp.scaffold</groupId>
                <artifactId>{{service.code.lower}}-dao</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.sp.scaffold</groupId>
                <artifactId>{{service.code.lower}}-service</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!--基础组件核心包-->
        <dependency>
            <groupId>com.sp.infra.comp</groupId>
            <artifactId>sp-infra-comp-logger</artifactId>
        </dependency>
        <dependency>
            <groupId>javax.validation</groupId>
            <artifactId>validation-api</artifactId>
        </dependency>

        {{sp-template-dependencies}}
    </dependencies>

</project>